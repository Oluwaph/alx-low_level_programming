#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings
 * @s1: parameter one
 * @s2: parameter two
 *
 * Return: dest
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (dest);
}
